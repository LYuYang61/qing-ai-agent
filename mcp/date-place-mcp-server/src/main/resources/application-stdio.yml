spring:
  config:
    activate:
      on-profile: stdio
  main:
    web-application-type: none
  ai:
    mcp:
      server:
        # stdio 由客户端拉起；HTTP transport 在该 Profile 中不会启用。
        stdio: true
        protocol: SSE
  logging:
    level:
      root: OFF
