spring:
  config:
    activate:
      on-profile: translation

qing:
  ai:
    rag:
      query-translation:
        enabled: true
        base-url: ${QING_AI_TRANSLATION_BASE_URL:https://libretranslate.com}
        api-key: ${LIBRETRANSLATE_API_KEY:}
        source-language: auto
        target-language: zh
